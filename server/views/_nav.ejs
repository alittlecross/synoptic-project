<nav>
  <% if (page === 'list' && permission === 'edit') { %>
    <a href="/add">Add</a>
  <% } %>
  <% if (page !== 'list') { %>
    <a href="/list">Back</a>
  <% } %>
  <a class="log-out" href="/log-out">Log Out</a>
</nav>
